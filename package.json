{
  "name": "shardfall",
  "version": "1.0.0",
  "description": "Shardfall — a top-down pixel dungeon-crawler RPG. Vanilla JS ES modules, no build step, no asset files.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "serve": "node serve.mjs",
    "build": "rm -rf dist && mkdir -p dist/js && cp index.html styles.css dist/ && cp js/*.js dist/js/",
    "test": "node smoke.mjs",
    "deploy:docker": "docker compose up -d"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0"
  }
}
